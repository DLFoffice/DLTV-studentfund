# ระบบดูแลนักเรียนทุนการศึกษาพระราชทานฯ DLTV — โครงสร้างใหม่

แยกโค้ดจากไฟล์ `index.html` ไฟล์เดียว (5,480 บรรทัด) ออกเป็นโมดูล
**โดยรักษาลำดับโค้ดเดิมทุกบรรทัด — พฤติกรรมของระบบเหมือนเดิม 100%**

## 📁 โครงสร้างโปรเจกต์

```
dltv-app/
├── index.html                  โครง HTML หลัก (เหลือ ~500 บรรทัด)
├── css/
│   └── style.css               สไตล์ทั้งหมด
├── js/                         ← โหลดตามลำดับเลขหน้าไฟล์ ห้ามสลับ
│   ├── 01-data.js              ข้อมูลนักเรียน 72 คน (⚠️ ลบทิ้งหลังย้ายเข้า Firestore)
│   ├── 02-state.js             ตัวแปรสถานะกลาง (DB, pagination)
│   ├── 03-utils.js             ฟังก์ชันช่วยเหลือ (format, วันที่ไทย, รูป)
│   ├── 04-navigation.js        เมนู sidebar / สลับหน้า
│   ├── 05-dashboard.js         แดชบอร์ด + กราф Chart.js
│   ├── 06-students-list.js     รายชื่อนักเรียน + filter + ค้นหา
│   ├── 07-student-detail.js    Modal รายละเอียด + แท็บย่อย
│   ├── 08-gpa-sheet.js         ตารางผลการเรียน + export
│   ├── 09-photos.js            อัปโหลด/จัดการรูปนักเรียน
│   ├── 10-storage.js           💾 ชั้นบันทึกข้อมูล (localStorage) ← จุดสลับเป็น Firestore
│   ├── 11-sheet-sync.js        ซิงก์ Google Sheet (Apps Script)
│   ├── 12-ui-misc.js           export JSON, แถบสถานะ, INIT, lightbox
│   ├── 13-map.js               แผนที่ Leaflet
│   ├── 14-student-forms.js     แบบฟอร์มติดตาม 1/2 + พิมพ์
│   ├── 15-form-tracking.js     หน้าติดตามการกรอกฟอร์ม
│   ├── 16-photo-urls.js        อัปเดต URL รูปจาก Excel
│   ├── 17-clock.js             นาฬิกา topbar
│   └── … 18–25 (ดูหัวข้อเวอร์ชันด้านล่าง; 25-teacher-worklist.js = หน้า "งานที่ต้องกรอก")
├── firebase/
│   ├── firebase-config.js      ← ใส่ค่าโปรเจกต์ Firebase ของคุณที่นี่
│   ├── firestore-service.js    ชั้นข้อมูล Firestore (window.fb)
│   ├── migrate.html            หน้าย้ายข้อมูล ใช้ครั้งเดียว (ไม่ควร deploy ขึ้นเว็บจริง)
│   └── firestore.rules         Security Rules (ต้องติดตั้ง!)
└── README.md
```

## ▶️ วิธีรันแอป (แบบเดิม ยังใช้ localStorage)

เปิดผ่าน local server (จำเป็น เพราะเบราว์เซอร์บล็อกบางอย่างเมื่อเปิดไฟล์ตรงๆ):

```bash
# ถ้ามี Python
cd dltv-app
python -m http.server 8000
# แล้วเปิด http://localhost:8000
```

หรือใช้ VS Code + ส่วนขยาย "Live Server" คลิกขวาที่ index.html → Open with Live Server

## ✏️ แก้ไข/เพิ่มฟีเจอร์ตรงไหน

| อยากแก้อะไร | ไปที่ไฟล์ |
|---|---|
| สี ฟอนต์ layout | `css/style.css` |
| การ์ด/ตารางรายชื่อ | `js/06-students-list.js` |
| หน้ารายละเอียดนักเรียน | `js/07-student-detail.js` |
| กราฟแดชบอร์ด | `js/05-dashboard.js` |
| วิธีเก็บข้อมูล | `js/10-storage.js` |
| แบบฟอร์ม 1/2 | `js/14-student-forms.js` |

กติกาเดียวที่ต้องจำ: **สคริปต์เหล่านี้แชร์ตัวแปรร่วมกันตามลำดับโหลด**
ถ้าเพิ่มไฟล์ใหม่ที่เรียกฟังก์ชันของไฟล์อื่น ให้วาง `<script>` ต่อท้ายใน index.html

---

## 🔥 การย้ายข้อมูลไป Firebase (Firestore)

### ขั้นที่ 1 — สร้างโปรเจกต์ Firebase
1. เข้า https://console.firebase.google.com → **Add project**
2. เมนู **Build → Firestore Database** → Create database → เลือก location `asia-southeast1 (Singapore)` → เริ่มในโหมด **production**
3. เมนู **Build → Authentication** → Get started → แท็บ Sign-in method → เปิด **Email/Password**
4. แท็บ **Users** → Add user → สร้างบัญชีให้เจ้าหน้าที่แต่ละคน (อีเมล + รหัสผ่าน)
5. หน้า Project Overview → ไอคอน `</>` (Web app) → ตั้งชื่อแอป → คัดลอก `firebaseConfig`

### ขั้นที่ 2 — ตั้งค่าในโปรเจกต์นี้
1. วางค่า config ลงใน `firebase/firebase-config.js`
2. Firebase Console → Firestore → แท็บ **Rules** → วางเนื้อหาจาก `firebase/firestore.rules` → **Publish**
   ⚠️ ขั้นนี้ห้ามข้าม — ไม่งั้นใครก็อ่านข้อมูลเด็กได้

### ขั้นที่ 3 — ย้ายข้อมูล (ครั้งเดียว)
1. เปิดไฟล์ `firebase/migrate.html` (ดับเบิลคลิกเปิดได้เลย ไม่ต้องรัน server)
   — ช่อง "บันทึกการทำงาน" จะตรวจความพร้อมให้อัตโนมัติ ถ้ามี ❌ ให้แก้ตามคำแนะนำก่อน
2. ล็อกอินด้วยบัญชีที่สร้างในขั้นที่ 1
3. กด **เริ่มย้ายข้อมูล** → รอจนขึ้น 🎉 ครบ 72 คน
4. ตรวจใน Firebase Console → Firestore → collection `students` ควรมี 72 documents

### ขั้นที่ 4 — สลับแอปหลักให้ใช้ Firestore ✅ ทำให้แล้ว
ไม่ต้องแก้โค้ดเอง — ระบบมีไฟล์ `js/18-firebase-bridge.js` เชื่อมให้อัตโนมัติ:

- ถ้า `firebase-config.js` **ยังเป็นค่าตัวอย่าง** → แอปทำงานแบบเดิม (localStorage) ทุกประการ
- ถ้า **ใส่ค่า config จริงแล้ว** → เปิดแอปจะเจอหน้าล็อกอินก่อน หลังล็อกอิน:
  - โหลดข้อมูลนักเรียนจาก Firestore มาแสดง
  - ถ้าคลาวด์ยังว่าง จะถามว่าอัปโหลดข้อมูลในเครื่องขึ้นไปเลยไหม
    (= ไม่ต้องใช้ migrate.html ก็ได้ ถ้าสะดวกทางนี้)
  - ทุกการแก้ไข/เพิ่ม/ลบ บันทึกขึ้นคลาวด์อัตโนมัติ
  - **realtime**: เปิดหลายเครื่องพร้อมกัน แก้ที่เครื่องหนึ่ง อีกเครื่องเห็นทันที
  - ปุ่ม "☁️ ออกจากระบบ" อยู่มุมล่างขวา
  - การดึงรายชื่อจาก Google Sheet ถูกปิด (กันข้อมูลตีกัน) แต่การส่งแบบฟอร์มไป Sheet ยังใช้ได้

### ขั้นที่ 5 — เก็บกวาดเรื่องความปลอดภัย (สำคัญที่สุด)
เมื่อยืนยันว่าแอปอ่านจาก Firestore ได้แล้ว:
1. **ลบเนื้อหาใน `js/01-data.js`** เหลือแค่ `const RAW = [];`
   → ข้อมูลเลขบัตร ปชช./บัญชีธนาคาร/ที่อยู่ของเด็กจะไม่ฝังอยู่ในโค้ดอีก
2. เพิ่มหน้าล็อกอินให้แอปหลัก (ใช้ `fb.signIn` / `fb.onAuth`)
3. ถ้า deploy ขึ้นเว็บ แนะนำ Firebase Hosting (`firebase deploy`) — ฟรีและได้ HTTPS

## 💡 หมายเหตุ
- Firestore ฟรีเทียร์ (Spark): อ่าน 50,000 ครั้ง/วัน เขียน 20,000 ครั้ง/วัน — เหลือเฟือสำหรับงานนี้
- รูปนักเรียนปัจจุบันใช้ Google Drive URL อยู่แล้ว ใช้ต่อได้เลย
  (ถ้าอยากย้ายรูปเข้า Firebase Storage ค่อยทำเป็นเฟสถัดไป)
- ระบบซิงก์ Google Sheet เดิม (`11-sheet-sync.js`) ยังทำงานคู่กันได้
  จนกว่าจะพร้อมเลิกใช้


---

## 🔐 ระบบบัญชีผู้ใช้และหน้า Login (ใหม่)

ไฟล์ที่เกี่ยวข้อง: `login.html`, `accounts.html`, `js/18-firebase-bridge.js` (v2), `firebase/firestore.rules` (v2)

### การไหลของระบบ
1. เปิด `index.html` โดยยังไม่ล็อกอิน → ถูกพาไป `login.html` (ดีไซน์ DLTV Fund)
2. ล็อกอินด้วย **อีเมล** หรือ **username** (เช่น dltv001) + รหัสผ่าน มีปุ่มตา 👁️ ดูรหัส,
   "จดจำฉันไว้ในระบบ", และ "ลืมรหัสผ่าน?" (ส่งลิงก์รีเซ็ตทางอีเมล)
3. ทุกการล็อกอินถูกบันทึกลง collection `loginLogs` (ใคร เข้าเมื่อไหร่)
4. ในแอปมีปุ่ม "☁️ บัญชี" มุมล่างขวา: เปลี่ยนรหัสผ่าน / จัดการบัญชี (แอดมิน) / ออกจากระบบ
5. บัญชีที่สร้างใหม่จะถูก**บังคับเปลี่ยนรหัสผ่าน**ในการเข้าครั้งแรก

### ตั้งค่าแอดมิน (ทำครั้งเดียว)
1. Firebase Console → Firestore → Start collection ชื่อ `admins`
2. Document ID = **อีเมลของคุณ** (ที่ใช้ล็อกอิน) → เพิ่มฟิลด์ `role` = `admin` → Save
3. **อัปเดต Security Rules** เป็นเวอร์ชันใหม่จากไฟล์ `firebase/firestore.rules` → Publish (สำคัญ!)

### สร้างบัญชีนักเรียนทุน 72 คน
1. ล็อกอินด้วยบัญชีแอดมิน → ปุ่ม ☁️ บัญชี → "จัดการบัญชีผู้ใช้" (หรือเปิด `accounts.html`)
2. กด "➕ สร้างบัญชีให้นักเรียนที่ยังไม่มี" — ระบบออก username `dltv001`–`dltv072`
   พร้อมรหัสเริ่มต้นสุ่ม (เช่น `Dltv4827`) ให้อัตโนมัติ
3. ตาราง "ข้อมูลการสมัคร" แสดง: ลำดับ | ชื่อ-สกุล | โรงเรียน | username | password (กดตา 👁️ ดูได้)
4. กด "⬇️ ส่งออก CSV" เพื่อพิมพ์แจกรหัสให้นักเรียน
5. หน้าเดียวกันมีตาราง "ประวัติการเข้าระบบล่าสุด" ให้ตรวจว่าใครเข้ามากรอกข้อมูลแล้วบ้าง

### สมัครสมาชิกด้วยตนเอง (register.html) — ใหม่
นักเรียนกดลิงก์ "สมัครสมาชิกนักเรียนทุน" จากหน้า login แล้วกรอก:
**ลำดับนักเรียน | ชื่อ-สกุลนักเรียนทุน | โรงเรียน | username | password** (มีตา 👁️ ดูรหัส)
ระบบจะตรวจลำดับ+ชื่อกับทะเบียนนักเรียนใน Firestore — ตรงจึงสมัครได้ และแต่ละลำดับสมัครได้ครั้งเดียว
บัญชีที่สมัครเองจะไม่ถูกบังคับเปลี่ยนรหัส (เพราะตั้งรหัสเองแล้ว) และแสดงในหน้า accounts.html ของแอดมินทันที
⚠️ ต้องใช้ Security Rules เวอร์ชันล่าสุด (ไฟล์ firestore.rules) จึงจะสมัครได้

### ปุ่มบัญชีย้ายไป sidebar (ล่างซ้าย)
ปุ่ม "🔑 รหัสผ่าน | 👥 บัญชี (แอดมิน) | 🚪 ออก" อยู่ใต้ปุ่ม Sync/Save/Export ใน sidebar แล้ว
และชื่อ-บทบาทมุมล่างซ้ายจะแสดงเป็นชื่อผู้ที่ล็อกอินจริง

### ข้อจำกัดที่ควรรู้
- เมื่อนักเรียน**เปลี่ยนรหัสเองแล้ว** จะไม่มีใครดูรหัสได้อีก (มาตรฐานความปลอดภัย)
  ถ้าลืม: ลบผู้ใช้คนนั้นใน Console → Authentication แล้วกดสร้างบัญชีใหม่ในหน้า accounts.html
- "ลืมรหัสผ่าน" ทางอีเมลใช้ได้เฉพาะบัญชีที่เป็นอีเมลจริง — บัญชี username นักเรียนให้แอดมินออกรหัสใหม่แทน
- ⚠️ บัญชีนักเรียนทุกคน**มองเห็นข้อมูลนักเรียนทั้งหมด**ในแอป (โครงสร้างแอปโหลดทั้งชุด)
  ถ้าต้องการให้นักเรียนเห็นเฉพาะข้อมูลตัวเอง ต้องปรับโครงสร้างเพิ่มเติม — แจ้งได้ครับ

### แก้ popup เด้งรัว (bridge v2)
เดิมทุกการบันทึก/echo จากคลาวด์ทำให้ popup "ข้อมูลอัปเดตจากคลาวด์" เด้งซ้ำๆ
ตอนนี้ระบบเทียบข้อมูลแบบ canonical: ถ้าข้อมูลเหมือนเดิมจะเงียบสนิท
ถ้ามีการแก้จากเครื่องอื่นจริงๆ จะวาดหน้าจอใหม่แบบเงียบและกะพริบจุด sync เท่านั้น — ไม่มี popup อีก


---

## 🎓 อัปเดตล่าสุด: prefill แบบฟอร์ม + โหมดนักเรียน

### 1) ข้อมูลนักเรียนเติมลงแบบฟอร์ม 1/2 อัตโนมัติ
ฟิลด์ที่มีข้อมูลในระเบียนนักเรียนจะถูกเติมให้เองเมื่อเปิดฟอร์ม (แก้เพิ่มได้ตามปกติ):
ชื่อ-สกุล, ชื่อเล่น, เลขบัตร, วันเกิด, ที่อยู่, โรงเรียน+อำเภอ+จังหวัด+สังกัด, บัญชีธนาคาร,
ครูที่ปรึกษา, **ผู้ปกครอง (ชื่อ/สกุล/เบอร์)**, **ภาคเรียนล่าสุด**, **ชื่อผู้อำนวยการ**,
**ยอดเงินทุนที่ได้รับรวม** (คำนวณจากประวัติการเบิกจ่าย)
และแก้บั๊กเดิม: ฟอร์มที่เคยเปิดก่อนข้อมูลคลาวด์มาแล้วค่าว่าง "ค้างถาวร"
ตอนนี้ระบบจะเติมให้ใหม่อัตโนมัติเมื่อฟิลด์นั้นยังว่างอยู่ (ค่าที่กรอกเองไม่ถูกทับ)

### 2) โหมดนักเรียน — เห็นเฉพาะของตัวเอง
บัญชี role = student (จากการสมัครเอง หรือแอดมินสร้างให้):
- เมนูเหลือ 2 หน้า: **📝 แบบฟอร์มทุนการศึกษา** และ **📋 ติดตามการกรอกแบบฟอร์ม**
  (เข้าแอปแล้วพาไปหน้าแบบฟอร์มทันที ปุ่ม Sync/Save/Export ถูกซ่อน)
- ข้อมูลที่โหลด/เห็น/แก้ = ระเบียนของตัวเองเท่านั้น และ realtime เฉพาะเอกสารตัวเอง
- **บังคับฝั่งเซิร์ฟเวอร์ด้วย Security Rules (v4)**: ต่อให้ดัดแปลงหน้าเว็บ
  ก็อ่าน/เขียนข้อมูลนักเรียนคนอื่นไม่ได้
- ครูเห็นสิ่งที่นักเรียนกรอกแบบ realtime และแก้กลับได้ตามปกติ

### ⚠️ ต้องอัปเดต Security Rules อีกครั้ง (v4)
Firebase Console → Firestore → Rules → วางจากไฟล์ `firebase/firestore.rules` → Publish
(ถ้าไม่อัปเดต: นักเรียนจะยังเห็นข้อมูลทุกคนเหมือนเดิม)

### แก้ไข: นักเรียนเห็นรายชื่อครบ 72 คนในหน้าแบบฟอร์ม (v5)
สาเหตุ: ข้อมูลทั้งชุดที่ค้างใน cache ของเบราว์เซอร์ (จากการใช้งานครั้งก่อนในเครื่องเดียวกัน)
ถูกวาดขึ้นก่อนการกรองสิทธิ์ และหน้าแบบฟอร์ม/สถานะไม่ถูกวาดซ้ำหลังกรอง แก้แล้วดังนี้:
- ล็อกอินบัญชีนักเรียนปุ๊บ ระบบกรองข้อมูลในเครื่องเหลือของตัวเอง "ทันที" ไม่รอคลาวด์
- เขียนทับ cache ในเครื่องด้วยข้อมูลที่กรองแล้ว (ข้อมูลคนอื่นถูกล้างออกจากเครื่องนั้น)
- หน้าแบบฟอร์มทุนฯ และติดตามการกรอก ถูกวาดซ้ำอัตโนมัติทุกครั้งที่ข้อมูลเปลี่ยน
- กดปุ่ม back ของเบราว์เซอร์กลับมา → บังคับโหลดหน้าใหม่เพื่อตรวจสิทธิ์ซ้ำเสมอ (กัน bfcache)
หมายเหตุ: เครื่องที่เคยเห็น 72 คนค้างอยู่ เปิดแอปใหม่ครั้งแรกอาจเห็นแวบหนึ่งก่อนระบบล้าง cache ให้ หลังจากนั้นจะเหลือของตัวเองถาวร

### v7 — ตัด popup ตอนเข้าระบบ + เครื่องมือซ่อมการเชื่อมบัญชี
- **popup ตอนเข้าระบบถูกเอาออกทั้งหมด** (โหลดจากคลาวด์สำเร็จ ฯลฯ) เหลือเฉพาะ popup
  เกี่ยวกับ "การบันทึกข้อมูล" (บันทึกไม่สำเร็จ / อัปโหลดครั้งแรก / เปลี่ยนรหัสผ่าน)
  ข้อความสำคัญตอนเข้าระบบเปลี่ยนเป็น "แถบเหลืองด้านบน" ปิดได้ ไม่บังหน้าจอ
- **อาการนักเรียนเห็นหน้าว่าง (0 คน)**: เกิดจาก studentId ในบัญชีชี้ไปเอกสารนักเรียน
  ที่ไม่มีอยู่แล้ว (เช่น เลขบัตรถูกแก้ / ลบ-เพิ่มนักเรียนใหม่หลังสมัคร)
  → หน้า "จัดการบัญชีผู้ใช้" (accounts.html) ตรวจให้อัตโนมัติ: ขึ้นแถบแดงแจ้งจำนวน
  และมีปุ่ม **🔧 ซ่อมการเชื่อม** ท้ายแถวสีแดง กดแล้วระบบจับคู่จากลำดับ+ชื่อให้ใหม่
  เสร็จแล้วให้นักเรียนรีเฟรชแอปหรือออก-เข้าใหม่

### v8 — แก้ popup "กำลังโหลดข้อมูลจาก Google Sheet" และข้อมูลหายหลัง refresh
สาเหตุ: โค้ดเริ่มระบบ (INIT) ยังยิงโหลดข้อมูลจาก Google Sheet ทุกครั้งที่เปิดหน้า
ก่อนที่ตัวเชื่อม Firebase จะทันปิด — และเมื่อ Sheet (ซึ่งว่าง/เก่าแล้ว) ตอบกลับ
มันเขียนทับข้อมูลบนจอ ทำให้เหมือน "ข้อมูลหาย" หลัง refresh
แก้ที่ต้นทาง: เมื่อ Firebase ถูกตั้งค่า ระบบจะข้ามการโหลดจาก Google Sheet โดยสิ้นเชิง
(popup นั้นจึงไม่ขึ้นอีก และข้อมูลจากคลาวด์ไม่ถูกทับ) — การส่งออกไป Sheet ยังใช้ได้ตามเดิม

### v9 — ซ่อมการเชื่อมบัญชีนักเรียน "อัตโนมัติ" + ลดการกระพริบตอนเข้า
- นักเรียนที่บัญชีเชื่อมไม่ตรง (เห็นหน้าว่าง 0 คน): ระบบค้นหาระเบียนของตัวเอง
  จาก "ลำดับนักเรียน" แล้วผูกบัญชีใหม่ให้เองทันทีตอนเข้าระบบ — ไม่ต้องรอแอดมินกดซ่อม
  (ต้องใช้ Security Rules v5 จากไฟล์ firestore.rules — เพิ่มสิทธิ์อ่านเฉพาะเอกสาร
  ที่ "ลำดับ" ตรงกับบัญชีตัวเองเท่านั้น เพื่อการซ่อมนี้)
- ลดการกระพริบ dashboard ตอน F5 ของบัญชีนักเรียน: ระบบจำบทบาทจากครั้งก่อน
  แล้วซ่อนเมนู/สลับหน้าแบบฟอร์มทันทีตั้งแต่เปิดหน้า
- ถ้าซ่อมอัตโนมัติไม่สำเร็จ แถบเหลืองจะแสดง studentId และลำดับของบัญชี
  เพื่อให้แอดมินตรวจใน Firestore ได้ตรงจุด

### v10 — เคสเลขบัตรนักเรียนถูกแก้ไข (พบจริงจาก check.html)
อาการ: เอกสารที่บัญชีผูกไว้มีอยู่ แต่ "เลขบัตรข้างใน" เป็นคนละเลข → แอปแสดงหน้าว่าง
ระบบใหม่จัดการอัตโนมัติตอนนักเรียนเข้าแอป: อ่านเอกสารเลขใหม่ (Rules อนุญาตเพราะ
ลำดับตรงกับบัญชี) → ลบเอกสารค้างเลขเก่า → ผูกบัญชีกับเลขใหม่ → ใช้ข้อมูลจริงต่อทันที
check.html ก็วินิจฉัยเคสนี้แยกให้ชัดเจนแล้ว (กล่อง 🔁 เลขบัตรถูกแก้ไข)

### v11 — ยึด "ลำดับ+ชื่อ" เป็นตัวยืนยันตัวตน (แก้ปัญหานักเรียนเห็นผิดคน)
นโยบายใหม่ของโหมดนักเรียน:
- เอกสารที่บัญชีผูกไว้ต้อง "ชื่อตรงกับบัญชี" จึงแสดง — ถ้าไม่ตรง ระบบค้นหาตัวจริง
  จากลำดับ+ชื่อแล้วผูกใหม่ให้เอง / ถ้าหาไม่เจอ แสดงหน้าว่างพร้อมคำอธิบาย
  (ยอมว่างดีกว่าแสดงข้อมูลของคนอื่น) — ยกเว้นนามสกุลยังตรงกัน (เคสครูแก้สะกดชื่อ) จึงคงเดิม
- ระบบไม่ลบ/ย้ายเอกสารอัตโนมัติอีกต่อไป (ป้องกันข้อมูลเสียหาย)
ฝั่งแอดมิน (accounts.html):
- ปุ่มใหม่ **🩺 ตรวจ+แก้การเชื่อมทั้งระบบ** — ไล่ตรวจทุกบัญชีนักเรียน จับคู่กับทะเบียน
  ด้วยชื่อ (และลำดับ) แล้วแก้การผูกให้อัตโนมัติทั้งชุด พร้อมรายงาน:
  บัญชีที่ต้องตรวจเอง, เอกสารที่ "รหัสเอกสาร" ไม่ตรง "เลขบัตรข้างใน", และลำดับที่ซ้ำ
- การผูกบัญชีทุกจุดใช้ "รหัสเอกสารจริง" แทนฟิลด์เลขบัตร (แม่นยำแม้เลขบัตรถูกแก้)

### v12 — อุดช่องรั่วที่ทำให้ยังเห็นข้อมูลคนอื่นแม้ระบบตรวจพบว่าผูกผิด
- cache ในเครื่อง: ตัวกรองตอนเข้าตรวจ "ชื่อ" ด้วย ไม่ใช่แค่เลข → ข้อมูลคนอื่นที่ค้างเครื่องไม่ขึ้นจอ และถูกล้างทิ้ง
- realtime: จะเปิดเฉพาะเมื่อการผูกผ่านการยืนยันตัวตนแล้วเท่านั้น + มียามชื่อกันอีกชั้น
- เมื่อผูกไม่สำเร็จ ระบบงดบันทึกขึ้นคลาวด์ทั้งหมด (กันเขียนทับเอกสารของคนอื่น)
- การค้นหาตัวจริงจาก "ลำดับ" ลองทั้งชนิดตัวเลขและข้อความ (ข้อมูลเก่าอาจเก็บคนละชนิด)

### v13 — กู้คืนข้อมูลลำดับ 1 + ป้องกันข้อมูลอ่อนไหวรั่วบน GitHub Pages
เหตุการณ์: ระเบียนลำดับ 1 (ด.ช.ฐิติวัสส์ บุตตะคุ) หายจาก Firestore
สาเหตุ: ตัวเก็บกวาดอัตโนมัติในเวอร์ชัน v10 ลบ "เอกสารที่รหัสไม่ตรงเลขบัตรข้างใน" ทิ้ง
ซึ่งเอกสารนั้นคือช่องเก็บข้อมูลของลำดับ 1 → v11 เป็นต้นไปถอดการลบอัตโนมัติออกทั้งหมดแล้ว
(ตรวจยืนยัน: ในโค้ดเหลือ .delete() เฉพาะตอนแอดมินสั่งลบนักเรียนเท่านั้น)

เครื่องมือกู้คืน: อยู่ในโฟลเดอร์แยก recovery-tools (restore.html + backup-students.json)
⚠️ ไฟล์เหล่านี้มีเลขบัตร/บัญชีธนาคาร — **ห้ามอัปขึ้น GitHub Pages** ใช้ในเครื่องผ่าน
http://localhost เท่านั้น (มี .gitignore กันไว้แล้วในโปรเจกต์หลัก)

---

## 🧠 อัปเดตใหม่: แบบประเมิน SDQ (ฉบับครูเป็นผู้ประเมิน)

ไฟล์ที่เกี่ยวข้อง: `js/23-sdq-form.js` (โหลดหลังสุด ไม่ต้องแก้ไฟล์อื่น)

- เมนูใหม่ในหมวด **"พฤติกรรมนักเรียน"**: 🧠 แบบประเมิน SDQ และ 📈 สรุปผล SDQ
- หน้ากรอก: เลือกนักเรียน → กรอก **ระดับชั้น / ภาคเรียน** ที่หัวฟอร์ม + ข้อคำถาม 25 ข้อ
  (ครบตามแบบประเมินฉบับกระดาษ) → ระบบคำนวณคะแนนและแปลผล **5 ด้าน**
  (อารมณ์ / เกเร / ไม่อยู่นิ่ง-สมาธิสั้น / สัมพันธ์กับเพื่อน / สัมพันธภาพทางสังคม)
  พร้อมคะแนนรวม 4 ด้าน ให้ทันทีแบบเรียลไทม์ ตามเกณฑ์ "ฉบับครู/ผู้ปกครองเป็นผู้ประเมิน"
  มีส่วนเสริม (ไม่บังคับ) สำหรับประเมินผลกระทบต่อชีวิตประจำวัน
- หน้าสรุปผล (Dashboard): การ์ดสรุปจำนวน + กราฟแท่ง/วงกลม (Chart.js) แยกตามด้านและกลุ่ม
  (ปกติ/เสี่ยง/มีปัญหา) พร้อมตารางรายคน ค้นหา/กรองตามกลุ่ม และ **ส่งออก CSV**
- **Export PDF รายบุคคล**: ปุ่ม "🖨️ พิมพ์ / PDF" จัดหน้ากระดาษสวยงาม (ใช้ layout เดียวกับ
  แบบฟอร์มทุนการศึกษา) — พิมพ์ผ่าน window.print() → Save as PDF
- **แอดมิน/ครูเปิด-ปิดการใช้งานฟอร์มนี้ได้** เหมือนฟอร์ม 1/2 เดิม (ปุ่มที่หน้าเลือกนักเรียน)
  สถานะซิงก์ผ่าน Firestore `settings/sdqFormAccess`
- ข้อมูลเก็บที่ `student.sdq[ภาคเรียน] = {...}` แยกตามภาคเรียนเหมือนฟอร์ม 1/2 และบันทึก
  อัตโนมัติผ่านกลไกเดิมของระบบ (localStorage + Firestore ถ้าตั้งค่าไว้) ไม่ต้องแก้ Security Rules เพิ่ม

---

## 🛡️ v15 — อัปเดตความปลอดภัย (Rules v7)

### ⚠️ ลำดับการติดตั้ง (ห้ามสลับ)
1. อัปโหลดไฟล์ JS/HTML ชุดนี้ขึ้นเว็บ **ก่อน**
2. Firebase Console → Firestore → Rules → วาง `firebase/firestore.rules` (v7) → **Publish**
   (ถ้า Publish ก่อน นักเรียนจะบันทึกแบบฟอร์มไม่ได้ เพราะโค้ดเก่าส่งทุกฟิลด์ขึ้นคลาวด์)
3. เปิด `accounts.html` → กด **📢 เผยแพร่ทะเบียนสมัคร (roster)** อีกครั้ง เพื่อเขียนทับ roster เก่าที่ยังมีชื่อจริง
4. (แนะนำ) ทดสอบ Rules ก่อน Publish: `cd tests && npm install && npm test` (ต้องมี Java 11+)

### สิ่งที่เปลี่ยน
| เรื่อง | ไฟล์ | รายละเอียด |
|---|---|---|
| กัน XSS | `js/03-utils.js`, 06, 07, 10, 11, 12 | `sanitizeStudent()` ล้าง `< > " ' \` ` \ ` ตั้งแต่ข้อมูลเข้าแอป + `escHtml()` ในจุดหลัก + เปิดรูปขยายผ่าน `data-lb-url` แทน `onclick` |
| นักเรียนแก้ได้เฉพาะแบบฟอร์ม | `firestore.rules`, `js/18-firebase-bridge.js` | ฟิลด์ที่แก้ได้: `form1`, `form2`, `forms`, `sdq` (ต้องตรงกันทั้งสองไฟล์) |
| บัญชีผู้ใช้ | `firestore.rules` | เจ้าของแก้ได้เฉพาะสถานะรหัสผ่าน / ลบรหัสเริ่มต้น / ผูก studentId |
| loginLogs | `firestore.rules` | uid ต้องตรงผู้เขียน + เวลาจากเซิร์ฟเวอร์ + ฟิลด์จำกัด |
| รหัสเริ่มต้น | bridge, `accounts.html` | ลบ `initialPassword` ทันทีเมื่อเปลี่ยนรหัส + เก็บกวาดอัตโนมัติเมื่อแอดมินเปิดหน้าบัญชี |
| ข้อมูลค้างเครื่อง | `js/10-storage.js`, bridge | โหมด Firebase ไม่เก็บข้อมูลนักเรียนใน localStorage + ล้างเมื่อออกจากระบบ |
| Google Sheet | `js/03-utils.js`, 15, bridge | โหมด Firebase ไม่ mirror ทั้งระเบียนไป Sheet / ฟอร์มที่ส่งไป Sheet ปิดบังเลขบัตร-เลขบัญชี (ตั้งค่า `SHEET_SEND_SENSITIVE`, `SHEET_MIRROR_IN_FIREBASE_MODE`) |
| roster | `accounts.html`, `request-code.html` | เก็บเฉพาะ SHA-256 ของชื่อ/นามสกุล — ไม่มีชื่อจริงให้คนนอกไล่อ่าน |
| ลบไฟล์ | `firebase/publish-roster.html` | ถูกแทนด้วยปุ่มใน accounts.html (ไฟล์เดิมจะเขียนชื่อจริงกลับเข้า roster) |
| อื่น ๆ | `.gitignore`, `tests/` | กันไฟล์ backup/CSV ขึ้น GitHub + ชุดทดสอบ Rules 23 เคส |

### ผลข้างเคียงที่ควรรู้
- ข้อความที่มีเครื่องหมาย `"` หรือ `'` จะแสดงเป็น `”` / `’` (ตั้งใจ — เพื่อความปลอดภัย)
- รูปที่อัปโหลดเป็น base64 ในเครื่อง (ไม่ใช่ลิงก์ Drive) จะไม่ถูกจำข้ามการรีเฟรชในโหมด Firebase — ใช้ลิงก์ Google Drive แทน
- ฟอร์มที่ส่งไป Sheet ครั้งแรกหลังอัปเดตอาจสร้างแถวใหม่ (เพราะ studentId ถูกปิดบัง)
- การส่ง "ทั้งระเบียน" ไป Sheet ในโหมด Firebase ถูกปิด — ใช้ข้อมูลบน Firestore เป็นหลัก

### ยังต้องทำเองนอกโค้ด
- ตรวจ Apps Script URL ว่าเปิดสาธารณะหรือไม่ (เปิดในหน้าต่างไม่ระบุตัวตน) — ถ้าได้ข้อมูลกลับมา ให้ปิด/ลบข้อมูลในชีต
- ยืนยันตัวตนครูนอกระบบก่อนกดอนุมัติคำขอรหัส
- ตรวจประวัติ git ว่าเคย commit ไฟล์ที่มีข้อมูลจริงหรือไม่ (ถ้ามี ใช้ `git filter-repo` และตั้ง repo เป็น private)
- เปิด Firestore Point-in-Time Recovery / scheduled export เพื่อสำรองข้อมูล

---

## 📝 v16 — หน้า "งานที่ต้องกรอก" (แทนหน้าเลือกนักเรียนของแบบฟอร์มทุน)

ไฟล์: `js/25-teacher-worklist.js` (ใหม่), `css/style.css` (ต่อท้าย), `js/23-sdq-form.js` (เพิ่ม `SDQ.openFor`), `index.html` (เพิ่ม script)

- รวมงาน 3 อย่างของนักเรียนแต่ละคนในภาคเรียนที่เปิดอยู่ไว้ที่เดียว: ① แบบฟอร์มที่ 1 ② แบบฟอร์มที่ 2 ③ แบบประเมิน SDQ
- แต่ละงานบอกสถานะด้วยสี: เทา = ยังไม่เริ่ม, เหลือง = ทำค้าง (มี % / จำนวนข้อ), เขียว = ครบ, น้ำเงิน = ส่งแล้ว, เส้นประ = ยังไม่เปิดให้กรอก
- ปุ่มสีน้ำเงิน "เริ่ม…/ทำต่อ…" พาไปงานถัดไป และเปิดที่ **ส่วนแรกที่ยังกรอกไม่ครบ** ทันที
- ตัวกรอง ทั้งหมด / ยังไม่เริ่ม / ทำค้างไว้ / ครบทุกงาน พร้อมจำนวน + ค้นหาชื่อ โรงเรียน หรือลำดับ
- บัญชีที่มีนักเรียนคนเดียว (ครูประจำตัวที่ใช้บัญชีนักเรียน) เห็นเป็นขั้นตอนแนวตั้งพร้อมคำอธิบายแต่ละงาน
- ปีการศึกษา/ภาคเรียนที่เติมในแบบฟอร์มอิง "ภาคเรียนที่เปิดใช้งาน" แล้ว (เดิมตายตัวเป็น 2568)
- หน้านี้อ่านสถานะอย่างเดียว ไม่แก้ข้อมูล — ต้องการกลับไปหน้าเดิม ให้ลบบรรทัด `<script src="js/25-teacher-worklist.js">` ใน index.html

---

## 🔗 v17 — เชื่อมข้อมูลทุกส่วนเข้าหากัน

ไฟล์: `js/26-data-links.js` (ใหม่), `js/18-firebase-bridge.js` (ส่งสัญญาณเมื่อข้อมูลคลาวด์มาถึง),
`js/15-form-tracking.js` (นับ SDQ), `js/14-student-forms.js` (แก้ช่องชื่อผู้อำนวยการ), `index.html`

| เส้นทาง | ก่อน | ตอนนี้ |
|---|---|---|
| ทะเบียน → แบบฟอร์ม | เติมเฉพาะช่องว่างครั้งแรก | ช่องที่ผู้ใช้ยังไม่ได้แก้เอง อัปเดตตามทะเบียนเสมอ |
| แบบฟอร์ม → ทะเบียน | ไม่มี | ช่องที่ผู้ใช้แก้เองกลายเป็นค่าล่าสุดของทะเบียน (ครู: ทันที / บัญชีนักเรียน: เครื่องครูนำขึ้นให้อัตโนมัติ) |
| เกรดในแบบฟอร์ม → GPA → กลุ่มการดูแล/Dashboard | ไม่มี | เติม GPA ของภาคเรียนให้ (source: form) ถ้าครูยังไม่ได้กรอกในหน้าผลการเรียน |
| ประวัติเบิกจ่าย → ยอดเงินทุนรวมในฟอร์ม 2 | เติมครั้งแรก | อัปเดตตามประวัติเบิกจ่ายถ้าไม่ได้แก้เอง |
| หน้าติดตามการกรอก | นับเฉพาะฟอร์ม 1/2 | นับครบ 3 งาน (ฟอร์ม 1, 2, SDQ) + ป้ายย่อยรายงาน |
| ชื่อผู้อำนวยการในฟอร์ม 2 | ว่างเสมอ (อ่านผิดช่อง) | ดึงจากข้อมูลโรงเรียนได้ถูกต้อง |

ช่องที่เชื่อมสองทาง: ชื่อเล่น, วันเกิด, เชื้อชาติ, ศาสนา, ที่อยู่ปัจจุบันทุกช่อง, โทรศัพท์นักเรียน,
ชื่อ/นามสกุล/โทรศัพท์ผู้ปกครอง, บัญชีธนาคาร 4 ช่อง, ครูที่ปรึกษา (ชื่อ/ตำแหน่ง/โทร),
ชื่อ/ตำบล/อำเภอ/จังหวัด/สังกัดของโรงเรียน, ชื่อผู้อำนวยการ

ตั้งใจไม่เชื่อมกลับ: ชื่อ-สกุลนักเรียนและเลขบัตร ปชช. (ใช้ยืนยันตัวตนบัญชี — แก้ที่ทะเบียนเท่านั้น),
ภูมิลำเนา, ภาคเรียน/ปีการศึกษา, ยอดเงินทุนรวม

กติกาที่ควรรู้
- ใช้กับแบบฟอร์มของ "ภาคเรียนที่เปิดใช้งาน" เท่านั้น แบบฟอร์มภาคที่ผ่านมาเป็นหลักฐาน ณ วันนั้น ไม่ถูกแก้ย้อนหลัง
- ช่องว่างในฟอร์มไม่ลบข้อมูลในทะเบียน
- ข้อมูลเก่าก่อนอัปเดตนี้ที่ทั้งสองฝั่งมีค่าแต่ไม่ตรงกัน จะไม่ถูกเขียนทับอัตโนมัติ (ไม่รู้ว่าฝั่งไหนใหม่กว่า)
  เมื่อมีการแก้ช่องนั้นครั้งถัดไป ระบบจะเชื่อมให้เอง
- GPA ที่ครูกรอกเองในหน้าผลการเรียน (มากกว่า 0) มาก่อนค่าจากฟอร์มเสมอ
- การเลือกคอลัมน์ปีในตารางผลการเรียน: หัวคอลัมน์ที่มีปี พ.ศ. ตรง → ระดับชั้นตรง (เช่น ม.2) → คอลัมน์ขวาสุด
  แนะนำให้ครูใส่หัวคอลัมน์เป็นปีการศึกษา เช่น "2568" เพื่อความแม่นยำ
- บัญชีนักเรียนแก้ข้อมูลในฟอร์ม → ทะเบียนจะอัปเดตเมื่อมีเครื่องของครู/แอดมินเปิดแอปอยู่หรือเปิดครั้งถัดไป

---

## 🖨️ v18 — รูปแบบพิมพ์ / PDF "เต็มตามแบบฟอร์ม" ใหม่

ไฟล์: `js/27-print-layout.js` (ใหม่ — แทน sfPrintForm1/sfPrintForm2), `css/style.css` (ต่อท้าย), `index.html` (ฟอนต์ Sarabun + script)

- คงลำดับหัวข้อและเลขข้อตามแบบฟอร์มกระดาษของมูลนิธิฯ (ฟอร์ม 1 ข้อ 1–9, ฟอร์ม 2 ข้อ 1–17)
- ข้อมูลจัดเป็นตาราง 12 คอลัมน์ ป้ายกำกับอยู่เหนือค่า — ไม่เบียดเป็นบรรทัดยาว
- ตัวเลือกดึงจาก schema จริง แสดงเป็นกล่องติ๊ก และ "อื่น ๆ" แสดงข้อความที่ระบุ (เดิมพิมพ์ `__other__` ออกมา)
- ข้อมูลบิดา / มารดา / ผู้ปกครอง / ที่อยู่ / ค่าใช้จ่าย จัดเป็นกลุ่มมีกรอบ
- ตัวเลขเงินมีจุลภาคและหน่วย ชิดขวาในตาราง, ฟอร์ม 2 สรุปการใช้จ่ายเป็นตารางยอดเงินพร้อมยอดคงเหลือ
- ตารางว่างเหลือบรรทัดให้เขียนมือ, คำตอบปลายเปิดว่างมีเส้นบรรทัดให้เขียน
- เอกสารไหลต่อเนื่อง ตัดหน้าอัตโนมัติ (ไม่มีครึ่งหน้าว่าง), หัวตารางซ้ำเมื่อข้ามหน้า
- ทุกหน้ามีหัวกระดาษ (ชื่อแบบฟอร์ม + ชื่อนักเรียน) และท้ายกระดาษ "หน้า x / y" + วันที่พิมพ์
  (ต้องใช้ Chrome / Edge รุ่น 131 ขึ้นไป — เบราว์เซอร์อื่นพิมพ์ได้ตามปกติแต่ไม่มีหัว/ท้ายกระดาษ)
- ฟอร์ม 2 มีหน้าปก → หน้าสรุปสาระสำคัญ (ลงนามครู + ผอ.) → รายละเอียด เหมือนแบบฟอร์มกระดาษ
- ฟอนต์ Sarabun (มาตรฐานหนังสือราชการ) โทนหมึกดำ-น้ำเงิน ประหยัดหมึก
- แท็บ "สรุปย่อ" และเอกสาร SDQ ใช้รูปแบบเดิม

ตอนพิมพ์: ในหน้าต่างพิมพ์ให้เลือก "ปลายทาง: บันทึกเป็น PDF", กระดาษ A4, ระยะขอบ "ค่าเริ่มต้น",
และติ๊ก "กราฟิกพื้นหลัง" (Background graphics) เพื่อให้กล่องติ๊กและหัวตารางมีสี

---

## 🔍 v19 — แก้ช่องค้นหาในหน้ารายชื่อนักเรียน

ไฟล์: `js/06-students-list.js`, `index.html`

- เดิมค้นได้แค่ ชื่อ / ชื่อเล่น / เลขบัตร / โรงเรียน → พิมพ์ชื่อจังหวัดถูกแต่ไม่ขึ้น
- ตอนนี้ค้นได้ทั้ง ลำดับ, ชื่อ, ชื่อเล่น, เลขบัตร, โรงเรียน, จังหวัด (ทั้งจังหวัดโรงเรียนและที่อยู่บ้าน), อำเภอ, ตำบล, สังกัด, เบอร์โทร, ชื่อผู้ปกครอง
- พิมพ์ "จ.ขอนแก่น", "จังหวัด ขอนแก่น", "อ.บ้านไผ่" ได้ / ไม่สนช่องว่าง / "ํา" = "ำ" / "กทม" = กรุงเทพ
- หลายคำคั่นด้วยเว้นวรรค = ต้องตรงทุกคำ เช่น "ขอนแก่น บ้านไผ่"
- พิมพ์ค้นหาหรือเปลี่ยนตัวกรองแล้วกลับไปหน้าแรกของผลลัพธ์เสมอ
- รายการจังหวัด/สังกัดในตัวกรองสร้างใหม่ทุกครั้งที่ข้อมูลเปลี่ยน (เดิมเติมครั้งเดียว ข้อมูลจากคลาวด์ที่มาทีหลังจึงไม่มีในรายการ)
  และรวม "จ.ขอนแก่น" กับ "ขอนแก่น" เป็นตัวเลือกเดียวกัน

---

## 🗺️ v20 — เลือกได้หลายจังหวัด + ส่งออกรายชื่อ

ไฟล์: `js/06-students-list.js`, `index.html`, `css/style.css`

- ตัวกรองจังหวัดเปลี่ยนเป็นกล่องเลือกหลายรายการ: มีช่องพิมพ์ค้นหาจังหวัด, จำนวนนักเรียนต่อจังหวัด,
  ปุ่ม "เลือกทั้งหมดที่แสดง" / "ล้างที่เลือก" — พิมพ์ชื่อแล้วกด Enter เพื่อเลือกได้เร็ว
- จังหวัดที่เลือกแสดงเป็นป้ายใต้แถบค้นหา พร้อมจำนวนคนรวม กด × เพื่อเอาออกทีละจังหวัด
- ช่องค้นหาข้อความ: ใช้ `,` คั่นเพื่อค้นแบบ "หรือ" เช่น `ขอนแก่น, อุดรธานี` (เว้นวรรค = "และ" เหมือนเดิม)
- ปุ่ม "📥 ส่งออกรายชื่อ (CSV)" ส่งออกเฉพาะรายชื่อตามตัวกรองปัจจุบัน เปิดด้วย Excel ได้ภาษาไทยถูกต้อง
  คอลัมน์: ลำดับ ชื่อ ชื่อเล่น โรงเรียน อำเภอ จังหวัด สังกัด GPA ล่าสุด กลุ่มการดูแล เบอร์โทร ผู้ปกครอง
  (ไม่รวมเลขบัตร ปชช. / เลขบัญชีธนาคาร / ที่อยู่บ้าน)
